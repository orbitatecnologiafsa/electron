package com.electron.repositories;

import com.electron.domain.TributoCest;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TributoCESTRepository extends JpaRepository<TributoCest, Long> {
}
